
def check_if_run_completed(run_name, experiment_name, directory=".")
    pass


def purge_runs(experiment_name, directory="."):
    pass


def analyses_index(experiment_name, directory="."):
    pass